🧪 Script de test (Vitest)

Ejemplo en package.json:

{
  "scripts": {
    "test": "cross-env NODE_ENV=test dotenv -e .env.test -- vitest"
  }
}


O más simple si usás dotenv/config:

{
  "scripts": {
    "test": "cross-env NODE_ENV=test vitest"
  }
}


Y en vitest.config.ts o setup:

import 'dotenv/config'
